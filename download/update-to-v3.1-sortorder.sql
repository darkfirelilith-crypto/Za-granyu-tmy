-- ============================================================
-- «За гранью тьмы» — обновление БД до v3.1
-- Добавлен ручной порядок дел архива «Зова Ктулху» (перетаскивание).
-- Идемпотентный скрипт: можно запускать повторно, данные не трогает.
-- Применять через Neon SQL Editor или psql (или prisma db push — уже применено).
-- ============================================================

ALTER TABLE "CocSheet"
  ADD COLUMN IF NOT EXISTS "sortOrder" INTEGER NOT NULL DEFAULT 0;
